var Observable = require('FuseJS/Observable');

var subject = {
	isChoice: Observable(),
	color: Observable(),
	type: Observable(),
	text: Observable()
};

var dsParam="";






var choiceSubjectList = Observable();














module.exports = {
	subject, dsParam
};